#include "app.hpp"

/*
auto app(int argc, char *argv[])
{
    Q_INIT_RESOURCE(app);
    QGuiApplication app(argc, argv);
    QQuickView view;
    view.setSource(QUrl("qrc:/main.qml"));
    return app.exec();
}
 */